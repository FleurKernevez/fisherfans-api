'use strict';


/**
 * Create
 * Create new reservation
 *
 * boatTrip_id   (optional)
 * choosenDate   (optional)
 * seatsBooked   (optional)
 * totalPrice   (optional)
 * user_id   (optional)
 * no response value expected for this operation
 **/
exports.createReservation = function(boatTrip_id,choosenDate,seatsBooked,totalPrice,user_id) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * Delete
 * Delete a reservation
 *
 * id  
 * no response value expected for this operation
 **/
exports.deleteReservation = function(id) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * Get
 * Get a reservation
 *
 * id   (optional)
 * boatTrip_id   (optional)
 * choosenDate   (optional)
 * seatsBooked   (optional)
 * totalPrice   (optional)
 * user_id   (optional)
 * no response value expected for this operation
 **/
exports.getReservation = function(id,boatTrip_id,choosenDate,seatsBooked,totalPrice,user_id) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * Update
 * Update values for about reservation
 *
 * id  
 * boatTrip_id   (optional)
 * choosenDate   (optional)
 * seatsBooked   (optional)
 * totalPrice   (optional)
 * user_id   (optional)
 * no response value expected for this operation
 **/
exports.updateByIdReservation = function(id,boatTrip_id,choosenDate,seatsBooked,totalPrice,user_id) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * Update
 * Update values for about reservation
 *
 * id  
 * boatTrip_id   (optional)
 * choosenDate   (optional)
 * seatsBooked   (optional)
 * totalPrice   (optional)
 * user_id   (optional)
 * no response value expected for this operation
 **/
exports.updateReservation = function(id,boatTrip_id,choosenDate,seatsBooked,totalPrice,user_id) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}

